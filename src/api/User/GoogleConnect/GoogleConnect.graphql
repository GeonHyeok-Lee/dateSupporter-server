type GoogleConnectResponse {
  ok: Boolean!
  error: String
  token: String
}

type Mutation {
  GoogleConnect(
    firstName: String!
    lastName: String!
    email: String!
    googleId: String!
  ): GoogleConnectResponse!
}

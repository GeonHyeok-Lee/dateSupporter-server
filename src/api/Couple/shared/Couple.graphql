type Couple {
  id: Int!
  status: String!
  chat: Chat
  requestedPhoneNumber: String!
  acceptedPhoneNumber: String!
  requestUser: User
  requestUserId: Int!
  acceptUser: User
  acceptUserId: Int!
  createdAt: String!
  updatedAt: String!
}

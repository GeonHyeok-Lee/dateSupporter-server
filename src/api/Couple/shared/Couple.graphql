type Couple {
  id: Int!
  status: String!
  chat: Chat
  requestUser: User
  requestUserId: Int!
  acceptUser: User
  acceptUserId: Int!
  requestPhoneNumber: String!
  acceptPhoneNumber: String!
  createdAt: String!
  updatedAt: String!
}

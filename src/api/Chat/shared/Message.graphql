type Message {
  id: Int!
  text: String!
  chat: Chat!
  chatId: Int!
  # requestUser: Couple
  # requestUserId: Int!
  # acceptUser: Couple
  # acceptUserId: Int!
  createdAt: String!
  updatedAt: String!
}

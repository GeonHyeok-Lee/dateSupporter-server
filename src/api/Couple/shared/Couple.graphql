type Couple {
  id: Int!
  status: String!
  chat: Chat
  searchPhoneNumber: String!
  requestUser: User
  requestUserId: Int!
  acceptUser: User
  acceptUserId: Int!
  createdAt: String!
  updatedAt: String!
}

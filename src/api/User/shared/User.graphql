type User {
  id: Int!
  fbId: String
  googleId: String
  kakaoId: String
  naverId: String
  email: String
  name: String
  firstName: String
  lastName: String
  fullName: String
  phoneNumber: String
  verifiedPhoneNumber: Boolean!
}

type Query {
  user: User
}
